[project]
name = "auction-monitor"
version = "0.2.0"
description = "Multi-source auction monitoring with DingTalk notification"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.55.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
